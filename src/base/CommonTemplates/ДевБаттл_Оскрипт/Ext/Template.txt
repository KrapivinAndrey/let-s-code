%1

Процедура Отладка(ВхЗначение)

	Попытка
		
		КЗаписи = Строка(ВхЗначение);
	
	Исключение
		
		КЗаписи = "Не удалось преобразовать в строку";
		
	КонецПопытки;
	
	Запись = Новый ЗаписьТекста("%4", КодировкаТекста.UTF16, , Истина);
	Запись.ЗаписатьСтроку(КЗаписи);
	Запись.Закрыть();
	
КонецПроцедуры
		
Результат = Ложь;

Попытка

	%2
	%3

Исключение
   
	Инфо = ИнформацияОбОшибке();
	_ОписаниеОшибки = Строка(Инфо.НомерСтроки) + ": " + Инфо.Описание;
	Сообщить(_ОписаниеОшибки);

КонецПопытки;

Если Не Результат Тогда
	Сообщить("Неверный ответ");
КонецЕсли;