Процедура ПриНачалеРаботыСистемы() Экспорт
	
	ДевБаттл_ОбщегоНазначенияВызовСервера.ИнициализацияПользователей();
	Если СтрНайти(ПараметрЗапуска, "upload") <> 0 ИЛИ СтрНайти(ПараметрЗапуска, "dump") <> 0 Тогда
		
		ДевБаттл_ПараметрЗапуска.Обработать(ПараметрЗапуска);
		ЗавершитьРаботуСистемы(Ложь);
		
	КонецЕсли;
	
КонецПроцедуры